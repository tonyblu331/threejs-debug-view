---
title: threejs-debug-view
description: Composable TSL debug views for Three.js WebGPU render pipelines.
template: splash
hero:
  tagline: Inspect Three.js WebGPU render output from one small debug layer.
  actions:
    - text: Quick Start
      link: /threejs-debug-view/guides/quick-start/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/tonyblu331/threejs-debug-view
      icon: external
---

`threejs-debug-view` gives you named debug views for the buffers your scene already produces: beauty, normals, depth, material channels, wireframe, lighting-only, reflection-only, overlap, and shader cost.

Use the root package for core helpers and TSL view definitions. Use `threejs-debug-view/r3f` when you want the overlay inside a React Three Fiber `<Canvas>`.

## R3F adapter

The R3F adapter is the React UI layer. `DebugViewLayer` mounts the default overlay in your canvas, reads the active R3F renderer, and wires the bundled Leva controls. Use `DebugViews` and `useDebugViewsControls` when your app needs to own that state.

Keep it behind your app's dev flag unless you intentionally publish a debug surface.

## Live demo

Use the controls in the top-right panel to switch views, layouts, opacity, and pane count. On the **Shader Cost** view, click the viewport to sample a pixel and align the legend marker with what you clicked.

[Open the demo in a new tab](/threejs-debug-view/demo/)

<iframe
  src="/threejs-debug-view/demo/"
  title="threejs-debug-view live demo"
  loading="eager"
  class="debug-demo-frame"
/>

:::note
The demo needs browser WebGPU support. When native WebGPU is unavailable, the app blocks startup instead of presenting a WebGL2 renderer as a valid debug backend.
:::

## What it does

- **Built-in sources** for beauty, normals, depth, albedo, roughness, AO, opacity, emissive, wireframe, lighting-only, reflection-only, overlap, and shader cost.
- **Render layouts** for single, overlay, split, diagonal breakdown, row, column, and grid presentations.
- **Explicit pane assignments** for fixed views, labels, per-pane cameras, and predictable resolution scaling.
- **Reusable runtime pipelines** for common material-channel switching without material mutation.
- **Custom TSL views** for shader-specific inspection without forking the built-in source list.
- **R3F adapter** for mounting the overlay and optional Leva controls inside a React Three Fiber app.

